# Derp.UI

UI editor app built on `Engine` (DerpTech2026).

Goal: a Figma-style vector/scene editor plus Rive-style animation tooling.

## Run

```bash
dotnet run --project Derp.UI/Derp.UI.csproj
```

