namespace DerpLib.Ecs.Setup;

public static class DerpEcsSetupConstants
{
    public const string ConditionalSymbol = "DERP_ECS_SETUP";
    public const string SyntaxOnlyMessage = "Derp.Ecs setup builders are syntax-only and must not be used at runtime.";
}
